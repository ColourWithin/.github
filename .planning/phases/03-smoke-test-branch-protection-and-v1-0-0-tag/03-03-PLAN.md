---
phase: 03-smoke-test-branch-protection-and-v1-0-0-tag
plan: 03
type: execute
wave: 2
depends_on:
  - "03-01"
  - "03-02"
files_modified:
  - README.md
  - .planning/phases/03-smoke-test-branch-protection-and-v1-0-0-tag/03-RELEASE-DANCE.md
  - .planning/phases/03-smoke-test-branch-protection-and-v1-0-0-tag/03-EXTERNAL-SMOKE-EVIDENCE.md
autonomous: true
requirements_addressed: [SMOKE-07, REL-02, REL-03, REL-04]
requirements:
  - SMOKE-07
  - REL-02
  - REL-03
  - REL-04

must_haves:
  truths:
    - "D-15 D-25 D-26 D-27: external colour-within-ops smoke evidence is a manual pre-tag gate; this plan prepares the release-candidate evidence files but does not create v1.0.0."
    - "D-21 D-22 D-23 D-24: top-level README contains a minimal internal v1.0.0 release note and links to detailed docs rather than duplicating a full tutorial."
    - "D-13 D-14: branch protection requires local checks only; no local OCI smoke job is required in .github branch protection."
    - "Addresses review concern: 03-RELEASE-DANCE.md is the single source of truth for candidate SHA choreography: merge release branch, capture main HEAD SHA, run ops smoke with that SHA, verify evidence, then tag that same SHA in Plan 03-04."
    - "Release evidence file records candidate SHA, colour-within-ops run URL, command, exit code, oci-cli-version, JSON parse result, and operator/date."
    - "README wording does not claim v1.0.0 is already released before the tag exists."
  artifacts:
    - path: "README.md"
      provides: "Minimal internal v1.0.0 release/consumption note"
      contains: "ColourWithin/.github/actions/oci-token-exchange@<sha>"
    - path: ".planning/phases/03-smoke-test-branch-protection-and-v1-0-0-tag/03-EXTERNAL-SMOKE-EVIDENCE.md"
      provides: "Manual release evidence record"
      contains: "colour-within-ops"
    - path: ".planning/phases/03-smoke-test-branch-protection-and-v1-0-0-tag/03-RELEASE-DANCE.md"
      provides: "Single source of truth for candidate SHA to smoke to tag choreography"
      contains: "git rev-parse origin/main"
---

<objective>
Prepare the v1.0.0 release-candidate gate: minimal top-level docs, explicit candidate-SHA choreography, external smoke evidence record, and local branch-protection verification.

This plan does not create the tag. Final tag creation moves to Plan 03-04 so the release branch can first land on `main`, the ops team can smoke the exact `main` candidate SHA, and the tag can target that same verified SHA.
</objective>

<execution_context>
@/Volumes/External/ColourWithin/dot-github/.codex/get-shit-done/workflows/execute-plan.md
@/Volumes/External/ColourWithin/dot-github/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@README.md
@docs/actions/README.md
@docs/actions/oci-ipt-smoke.md
@.planning/phases/03-smoke-test-branch-protection-and-v1-0-0-tag/03-CONTEXT.md
@.planning/phases/03-smoke-test-branch-protection-and-v1-0-0-tag/03-RESEARCH.md
@.planning/phases/03-smoke-test-branch-protection-and-v1-0-0-tag/03-VALIDATION.md
@.github/workflows/test-actions.yml
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-3-08 tag is created before external smoke evidence exists | high | This plan creates no tag command; Plan 03-04 owns final tagging after post-merge smoke evidence |
| T-3-09 branch protection requires an impossible external check | high | Verify protection requires only local `.github` check contexts |
| T-3-10 top-level docs overpromise real self-smoke or omit SHA-pin guidance | medium | README release note is minimal, explicit about SHA-pinned consumption, and links to detailed docs |
| T-3-11 candidate SHA drift between local branch, main, ops smoke, and tag | high | 03-RELEASE-DANCE.md records the authoritative candidate SHA and equality checks for Plan 03-04 |
</threat_model>

<decision_coverage>
- D-13 and D-14 are covered by branch protection verification.
- D-15 and D-25 through D-27 are covered by the release choreography and external evidence file; final tag checkpoint is in Plan 03-04.
- D-21 through D-24 are covered by top-level README release note.
</decision_coverage>

<tasks>

<task type="auto">
  <name>Task 1: Add minimal top-level v1.0.0 action release note</name>
  <files>README.md</files>
  <read_first>
    - README.md
    - docs/actions/README.md
    - .planning/phases/03-smoke-test-branch-protection-and-v1-0-0-tag/03-CONTEXT.md
  </read_first>
  <action>
Update `README.md` with a concise section near the layout or after the introduction:

```markdown
## First-party GitHub Actions

This repo also publishes internal ColourWithin GitHub Actions:

- `ColourWithin/.github/actions/oci-token-exchange@<sha>` - exchange GitHub Actions OIDC for an OCI UPST through IPT.
- `ColourWithin/.github/actions/run-oci-cli-command@<sha>` - run OCI CLI commands through argv-safe execution and captured outputs.

Consumers pin a commit SHA. The doubled `.github` path is expected because this is the organisation default repository. Detailed usage, prerequisites, and smoke-test guidance live in [docs/actions/](docs/actions/).

Release candidate: `v1.0.0` is tagged only after local checks pass and the `colour-within-ops` IPT smoke evidence records the exact candidate SHA.
```

Keep this minimal; do not paste the full smoke workflow or all action prerequisites into the top-level README.
  </action>
  <verify>
    <automated>rg "First-party GitHub Actions|ColourWithin/.github/actions/oci-token-exchange@<sha>|ColourWithin/.github/actions/run-oci-cli-command@<sha>|docs/actions/|v1.0.0" README.md</automated>
  </verify>
  <acceptance_criteria>
    - `README.md` contains `## First-party GitHub Actions`
    - `README.md` contains `ColourWithin/.github/actions/oci-token-exchange@<sha>`
    - `README.md` contains `ColourWithin/.github/actions/run-oci-cli-command@<sha>`
    - `README.md` contains `Consumers pin a commit SHA`
    - `README.md` contains `docs/actions/`
    - `README.md` contains `v1.0.0`
    - `README.md` does not contain `Current release: \`v1.0.0\``
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create release choreography single-source-of-truth doc</name>
  <files>.planning/phases/03-smoke-test-branch-protection-and-v1-0-0-tag/03-RELEASE-DANCE.md</files>
  <read_first>
    - .planning/phases/03-smoke-test-branch-protection-and-v1-0-0-tag/03-REVIEWS.md
    - .planning/phases/03-smoke-test-branch-protection-and-v1-0-0-tag/03-CONTEXT.md
    - docs/actions/oci-ipt-smoke.md
  </read_first>
  <action>
Create `.planning/phases/03-smoke-test-branch-protection-and-v1-0-0-tag/03-RELEASE-DANCE.md` as the single source of truth for the candidate SHA flow. It must contain these exact sections:

```markdown
# Phase 3 Release Choreography

## Sequence

1. Merge the Phase 3 release branch to `main`.
2. Fetch and record the authoritative candidate SHA:
   `git fetch origin main --tags`
   `git rev-parse origin/main`
3. Run `colour-within-ops/.github/workflows/oci-ipt-smoke.yml` with `candidate_sha=<origin/main-sha>`.
4. Record the workflow run URL and results in `03-EXTERNAL-SMOKE-EVIDENCE.md`.
5. Verify the smoke workflow input SHA equals `git rev-parse origin/main`.
6. Verify local branch protection requires only local `.github` checks.
7. Plan 03-04 creates signed tag `v1.0.0` at that same SHA after explicit user approval.

## Equality Checks

- `03-EXTERNAL-SMOKE-EVIDENCE.md` candidate SHA equals `git rev-parse origin/main`
- `colour-within-ops` workflow input `candidate_sha` equals `git rev-parse origin/main`
- `git rev-list -n 1 v1.0.0` equals `git rev-parse origin/main` after tag creation

## Non-Negotiables

- Do not tag a local-only commit that has not landed on `main`.
- Do not tag a commit different from the SHA smoke-tested by `colour-within-ops`.
- Do not create an unsigned `v1.0.0` tag.
- Do not require `colour-within-ops` checks in `.github` branch protection.
```

Addresses review concern: candidate SHA flow needs a single source of truth and structural release choreography.
  </action>
  <verify>
    <automated>rg "git rev-parse origin/main|workflow input `candidate_sha`|git rev-list -n 1 v1.0.0|Do not create an unsigned" .planning/phases/03-smoke-test-branch-protection-and-v1-0-0-tag/03-RELEASE-DANCE.md</automated>
  </verify>
  <acceptance_criteria>
    - `03-RELEASE-DANCE.md` contains `git rev-parse origin/main`
    - `03-RELEASE-DANCE.md` contains `workflow input \`candidate_sha\``
    - `03-RELEASE-DANCE.md` contains `git rev-list -n 1 v1.0.0`
    - `03-RELEASE-DANCE.md` contains `Do not create an unsigned \`v1.0.0\` tag`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Create external colour-within-ops smoke evidence template</name>
  <files>.planning/phases/03-smoke-test-branch-protection-and-v1-0-0-tag/03-EXTERNAL-SMOKE-EVIDENCE.md</files>
  <read_first>
    - docs/actions/oci-ipt-smoke.md
    - .planning/phases/03-smoke-test-branch-protection-and-v1-0-0-tag/03-CONTEXT.md
    - .planning/phases/03-smoke-test-branch-protection-and-v1-0-0-tag/03-RELEASE-DANCE.md
  </read_first>
  <action>
Create `.planning/phases/03-smoke-test-branch-protection-and-v1-0-0-tag/03-EXTERNAL-SMOKE-EVIDENCE.md` with this exact structure:

```markdown
# Phase 3 External Smoke Evidence

**Status:** pending
**Candidate `.github` SHA:** <fill with `git rev-parse origin/main` after release branch merge>
**Consumer repo:** `ColourWithin/colour-within-ops`
**Workflow:** `.github/workflows/oci-ipt-smoke.yml`
**Environment:** `production`
**Audience:** `https://github.com/ColourWithin/colour-within-ops`
**Command:** `oci os ns get`

## Required Before Tag

- [ ] Workflow run URL recorded
- [ ] Candidate SHA in workflow input matches this repo commit
- [ ] `actions/oci-token-exchange` used `@<candidate-sha>`
- [ ] `actions/run-oci-cli-command` used `@<candidate-sha>`
- [ ] Token exchange step passed
- [ ] OCI command exit code was `0`
- [ ] OCI command stdout parsed as JSON
- [ ] `oci-cli-version` was `>=3.81.1`
- [ ] No deploy or mutation step ran

## Evidence

- Workflow run URL:
- Run date:
- Operator:
- `exit-code`:
- `oci-cli-version`:
- JSON parse result:
- Notes:
```

If the external smoke has not been run yet, leave status `pending` and stop execution at the checkpoint. If evidence is already available, fill every field and set status to `passed`.
  </action>
  <verify>
    <automated>rg "git rev-parse origin/main|Candidate `.github` SHA|ColourWithin/colour-within-ops|oci os ns get|oci-cli-version" .planning/phases/03-smoke-test-branch-protection-and-v1-0-0-tag/03-EXTERNAL-SMOKE-EVIDENCE.md</automated>
  </verify>
  <acceptance_criteria>
    - `03-EXTERNAL-SMOKE-EVIDENCE.md` contains `Candidate \`.github\` SHA`
    - `03-EXTERNAL-SMOKE-EVIDENCE.md` contains `ColourWithin/colour-within-ops`
    - `03-EXTERNAL-SMOKE-EVIDENCE.md` contains `.github/workflows/oci-ipt-smoke.yml`
    - `03-EXTERNAL-SMOKE-EVIDENCE.md` contains `https://github.com/ColourWithin/colour-within-ops`
    - `03-EXTERNAL-SMOKE-EVIDENCE.md` contains `oci os ns get`
    - `03-EXTERNAL-SMOKE-EVIDENCE.md` contains `oci-cli-version`
  </acceptance_criteria>
</task>

<task type="manual">
  <name>Task 4: Verify branch protection requires only local checks</name>
  <files>.planning/phases/03-smoke-test-branch-protection-and-v1-0-0-tag/03-EXTERNAL-SMOKE-EVIDENCE.md</files>
  <read_first>
    - .github/workflows/test-actions.yml
    - .planning/phases/03-smoke-test-branch-protection-and-v1-0-0-tag/03-EXTERNAL-SMOKE-EVIDENCE.md
  </read_first>
  <action>
After local checks exist on the pushed PR/main commit, inspect ruleset-based branch protection first:

```bash
gh api repos/ColourWithin/.github/rulesets
gh api repos/ColourWithin/.github/rulesets/<id>
gh api orgs/ColourWithin/rulesets/<id>
```

Treat an active repository or organisation ruleset targeting the default branch as the authoritative protection source. The legacy endpoint is a secondary diagnostic only:

```bash
gh api repos/ColourWithin/.github/branches/main/protection
```

If the legacy endpoint returns `403` or `404` while an active ruleset protects the default branch, record that fallback behaviour instead of failing the task. If neither a ruleset nor legacy branch protection is present, do not half-apply changes. Record the failure and produce the exact ruleset-aware `gh api -X PUT` payload or GitHub UI steps needed for a repo or organisation admin to apply protection.

Required checks, if later added to a ruleset, must be local `.github` checks only, such as the `Unit tests and lint` job or the final check context emitted by `.github/workflows/test-actions.yml`. Do not require `colour-within-ops` workflow contexts directly.

Record in `03-EXTERNAL-SMOKE-EVIDENCE.md` notes whether branch protection was verified and which local check context is required.
  </action>
  <verify>
    <manual>`gh api repos/ColourWithin/.github/rulesets` or `gh api orgs/ColourWithin/rulesets/<id>` shows an active ruleset protecting the default branch; any required status checks belong to this repo's workflow, not `colour-within-ops`.</manual>
  </verify>
  <acceptance_criteria>
    - Branch protection notes in `03-EXTERNAL-SMOKE-EVIDENCE.md` include the required local check context name
    - Branch protection notes do not name `colour-within-ops` as a required status check context for this repo
  </acceptance_criteria>
</task>

</tasks>

<verification>
Plan passes when the README has a minimal release note, the release choreography file exists, the evidence template exists, and branch protection is verified against local checks only. Final tag creation is intentionally deferred to Plan 03-04.
</verification>

<success_criteria>
- REL-02 and REL-04 are reflected in the minimal README and detailed docs from Plan 03-02.
- SMOKE-07 is satisfied by branch protection requiring local release-readiness checks, with external smoke evidence as a manual pre-tag gate.
- Candidate SHA choreography is explicit before external smoke is requested.
- This plan contains no `git tag` command.
</success_criteria>
