---
phase: 03-smoke-test-branch-protection-and-v1-0-0-tag
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .github/workflows/test-actions.yml
autonomous: true
requirements_addressed: [SMOKE-01, SMOKE-06, SMOKE-07, REL-03]
requirements:
  - SMOKE-01
  - SMOKE-06
  - SMOKE-07
  - REL-03

must_haves:
  truths:
    - "D-01 D-13 D-14 D-15 D-16: .github branch protection requires only local truthful checks; real OCI smoke runs in colour-within-ops, not this repo."
    - "D-17 D-18 D-19: third-party workflow actions are full-SHA pinned, actionlint tarball download is checksum-verified, and latest versions are re-checked during execution."
    - "D-20: local workflows do not use local paths or tags for release proof; external smoke uses candidate .github commit SHA."
    - "SMOKE requirements are amended so SMOKE-02..05 point to external colour-within-ops smoke evidence rather than impossible local self-smoke."
    - "test-actions.yml keeps existing Python, npm, actionlint, injection, no-xtrace, audit, license, build, dirty-dist, and no-shell-exec gates."
    - "test-actions.yml includes actions/cache pinned to a full commit SHA with a comment naming the checked version."
    - "test-actions.yml verifies actionlint tarball SHA256 before extracting."
    - "test-actions.yml contains no local job that calls actions/oci-token-exchange against real OCI from this .github repo."
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Phase 3 requirements corrected for the cross-repo smoke boundary"
      contains: "colour-within-ops"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 3 plan list and success criteria corrected for honest local checks plus external smoke evidence"
      contains: "external smoke"
    - path: ".github/workflows/test-actions.yml"
      provides: "Local release-readiness workflow"
      contains: "sha256sum -c"
  key_links:
    - from: ".github/workflows/test-actions.yml"
      to: "actions/oci-token-exchange/requirements.txt"
      via: "pip cache key uses hashFiles('actions/oci-token-exchange/requirements.txt')"
      pattern: "hashFiles\\('actions/oci-token-exchange/requirements.txt'\\)"
    - from: ".github/workflows/test-actions.yml"
      to: "actions/run-oci-cli-command/dist/index.js"
      via: "dirty-dist verification after npm build"
      pattern: "git diff --exit-code -- actions/run-oci-cli-command/dist/index.js"
---

<objective>
Correct Phase 3's local repo contract and harden `.github/workflows/test-actions.yml` as the required local release-readiness check.

This plan deliberately removes the stale implication that `ColourWithin/.github` can run the real OCI token-exchange smoke itself. The real OCI smoke belongs to `colour-within-ops`; this repo requires local checks that can actually run here.
</objective>

<execution_context>
@/Volumes/External/ColourWithin/dot-github/.codex/get-shit-done/workflows/execute-plan.md
@/Volumes/External/ColourWithin/dot-github/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/03-smoke-test-branch-protection-and-v1-0-0-tag/03-CONTEXT.md
@.planning/phases/03-smoke-test-branch-protection-and-v1-0-0-tag/03-RESEARCH.md
@.planning/phases/03-smoke-test-branch-protection-and-v1-0-0-tag/03-VALIDATION.md
@.planning/phases/03-smoke-test-branch-protection-and-v1-0-0-tag/03-PATTERNS.md
@.github/workflows/test-actions.yml

<interfaces>
Current verified dependency refs from 2026-05-09 research:

```text
actions/checkout v6.0.2 -> de0fac2e4500dabe0009e67214ff5f5447ce83dd
actions/cache v5.0.5 -> 27d5ce7f107fe9357f9df03efb73ab90386fccae
rhysd/actionlint v1.7.12 -> tag 914e7df21a07ef503a81201c76d2b11c789d3fca
oci latest -> 2.173.1
oci-cli latest -> 3.81.1
```

Execution must re-check these before editing:

```bash
gh release view --repo actions/cache --json tagName,publishedAt,url
gh release view --repo actions/checkout --json tagName,publishedAt,url
gh release view --repo rhysd/actionlint --json tagName,publishedAt,url
git ls-remote https://github.com/actions/cache.git refs/tags/<latest-tag>
git ls-remote https://github.com/actions/checkout.git refs/tags/<latest-tag>
python -m pip index versions oci-cli
python -m pip index versions oci
```
</interfaces>
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-3-01 workflow supply-chain drift through tag-pinned third-party actions | high | Require full commit SHA refs for all third-party `uses:` entries and add static check forbidding tag-like refs |
| T-3-02 actionlint tarball tampering | high | Pin actionlint version and verify SHA256 before extracting |
| T-3-03 false release confidence from local fake OCI smoke | high | Amend requirements/roadmap and assert no local job attempts real OCI token exchange from `.github` |
| T-3-04 dependency floor staleness | medium | Re-check latest GitHub releases and PyPI versions during execution before pinning |
</threat_model>

<decision_coverage>
- D-01, D-13, D-14, D-15, D-16 are covered by requirements/roadmap corrections and local-only branch-protection wording.
- D-17, D-18, D-19 are covered by workflow pinning, actionlint checksum verification, and latest-version preflight.
- D-20 is covered by keeping release proof in the external candidate-SHA workflow rather than local path usage.
</decision_coverage>

<tasks>

<task type="auto">
  <name>Task 1: Re-check current dependency versions before workflow edits</name>
  <files>.github/workflows/test-actions.yml</files>
  <read_first>
    - .planning/phases/03-smoke-test-branch-protection-and-v1-0-0-tag/03-RESEARCH.md
    - .github/workflows/test-actions.yml
  </read_first>
  <action>
Run the latest-version checks from the `<interfaces>` block. If any of `actions/cache`, `actions/checkout`, `rhysd/actionlint`, `oci`, or `oci-cli` has a newer stable release than the values in research, use the newer stable value in subsequent tasks and record the observed version in the task summary. Do not downgrade existing pins.

Addresses review concern: verify PyPI output is a stable semantic version before adopting it. Treat versions containing `a`, `b`, `rc`, or `dev` suffixes as pre-release and do not adopt them unless they are the only available security fix and the user explicitly approves.
  </action>
  <verify>
    <automated>gh release view --repo actions/cache --json tagName,publishedAt,url && gh release view --repo actions/checkout --json tagName,publishedAt,url && gh release view --repo rhysd/actionlint --json tagName,publishedAt,url && python -m pip index versions oci-cli | head -1 && python -m pip index versions oci | head -1</automated>
  </verify>
  <acceptance_criteria>
    - Task summary records observed latest tags for `actions/cache`, `actions/checkout`, and `rhysd/actionlint`
    - Task summary records observed latest PyPI versions for `oci` and `oci-cli`
    - Task summary states whether the observed PyPI versions are stable releases
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Correct Phase 3 requirements and roadmap for external smoke ownership</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - .planning/phases/03-smoke-test-branch-protection-and-v1-0-0-tag/03-CONTEXT.md
  </read_first>
  <action>
Update `.planning/REQUIREMENTS.md` Phase 3 requirement text so:
- `SMOKE-01` remains local: `.github/workflows/test-actions.yml` triggers on PR-to-main when files under `actions/**`, `docs/actions/**`, `README.md`, or `.github/workflows/test-actions.yml` change.
- `SMOKE-02` says the external `colour-within-ops/.github/workflows/oci-ipt-smoke.yml` workflow declares `permissions: { id-token: write, contents: read }`.
- `SMOKE-03` says external smoke exercises `ColourWithin/.github/actions/oci-token-exchange@<candidate-sha>` from `colour-within-ops`, using the `production` environment, audience `https://github.com/ColourWithin/colour-within-ops`, and the ops repo's OCI client/domain values.
- `SMOKE-04` says external smoke exercises `ColourWithin/.github/actions/run-oci-cli-command@<candidate-sha>` chained after token exchange.
- `SMOKE-05` says first-choice read command is `oci os ns get`, with fallback to a documented read-only permission-scoped command if policy proof requires it.
- `SMOKE-06` says the local `.github` workflow uses SHA-pinned `actions/cache` for pip cache keyed on `hashFiles('actions/oci-token-exchange/requirements.txt')`.
- `SMOKE-07` says branch protection in `.github` requires local release-readiness checks only, while external smoke evidence is a manual pre-tag gate.

Update `.planning/ROADMAP.md` Phase 3 goal, external dependency, success criteria, and plan list to match the same truth: local checks here, external smoke in `colour-within-ops`, manual tag after both gates.
  </action>
  <verify>
    <automated>rg "colour-within-ops|oci-ipt-smoke|external smoke|oci os ns get|local release-readiness" .planning/REQUIREMENTS.md .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` contains `colour-within-ops/.github/workflows/oci-ipt-smoke.yml`
    - `.planning/REQUIREMENTS.md` contains `https://github.com/ColourWithin/colour-within-ops`
    - `.planning/REQUIREMENTS.md` contains `oci os ns get`
    - `.planning/REQUIREMENTS.md` contains `local release-readiness checks only`
    - `.planning/ROADMAP.md` Phase 3 section contains `external smoke`
    - `.planning/ROADMAP.md` Phase 3 section does not claim this repo's workflow runs real OCI token exchange
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Harden local release-readiness workflow pins, cache, and static checks</name>
  <files>.github/workflows/test-actions.yml</files>
  <read_first>
    - .github/workflows/test-actions.yml
    - actions/oci-token-exchange/requirements.txt
    - actions/run-oci-cli-command/package.json
  </read_first>
  <action>
Update `.github/workflows/test-actions.yml` while preserving existing gates.

Required workflow changes:
- Keep `on.pull_request.branches: [main]`.
- Extend `on.pull_request.paths` to include `docs/actions/**` and `README.md` in addition to `actions/**` and `.github/workflows/test-actions.yml`.
- Add `permissions: { contents: read }` to the local job if not already present.
- Keep `actions/checkout` pinned to the latest verified full commit SHA with a version comment.
- Add a pip cache step before Python dependency install using `actions/cache@<latest-full-sha>  # <version>` with:
  - `path: ~/.cache/pip`
  - key including `${{ runner.os }}-pip-${{ hashFiles('actions/oci-token-exchange/requirements.txt', 'actions/oci-token-exchange/pyproject.toml') }}`
- Keep Python install command using `pip install 'oci>=2.173.1,<3' requests cryptography pytest ruff` unless Task 1 found a newer `oci` floor that must be adopted consistently.
- Replace actionlint download with version-pinned URL plus SHA256 verification before `tar -xzf`. The run block must include `sha256sum -c`.
- Add a static check named like `Lint - third-party actions are SHA pinned` that scans `uses:` lines with a simple line-oriented rule. For every non-local `uses:` value that does not start with `./`, require the ref after `@` to match exactly `[0-9a-f]{40}`. This addresses review concern: avoid brittle YAML parsing and make the allowlist concrete.
- Add a static check named like `Lint - no local real OCI smoke in .github repo` that fails if `.github/workflows/test-actions.yml` contains both `oci-token-exchange` and `OCI_OIDC_CLIENT_IDENTIFIER`. This prevents reintroducing false local self-smoke.
  </action>
  <verify>
    <automated>actionlint .github/workflows/test-actions.yml && rg "actions/cache@[0-9a-f]{40}" .github/workflows/test-actions.yml && rg "sha256sum -c" .github/workflows/test-actions.yml && rg "hashFiles\\('actions/oci-token-exchange/requirements.txt', 'actions/oci-token-exchange/pyproject.toml'\\)" .github/workflows/test-actions.yml</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/test-actions.yml` contains `docs/actions/**`
    - `.github/workflows/test-actions.yml` contains `README.md`
    - `.github/workflows/test-actions.yml` contains `actions/cache@` followed by a 40-character SHA
    - `.github/workflows/test-actions.yml` contains `hashFiles('actions/oci-token-exchange/requirements.txt', 'actions/oci-token-exchange/pyproject.toml')`
    - `.github/workflows/test-actions.yml` contains `sha256sum -c`
    - `.github/workflows/test-actions.yml` contains `third-party actions are SHA pinned`
    - `.github/workflows/test-actions.yml` contains `[0-9a-f]{40}`
    - `.github/workflows/test-actions.yml` contains `no local real OCI smoke`
    - `actionlint .github/workflows/test-actions.yml` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Run local release-readiness verification</name>
  <files>.github/workflows/test-actions.yml</files>
  <read_first>
    - .github/workflows/test-actions.yml
    - actions/oci-token-exchange/pyproject.toml
    - actions/run-oci-cli-command/package.json
  </read_first>
  <action>
Run local verification that mirrors the updated workflow as closely as practical:

```bash
python -m pip install 'oci>=2.173.1,<3' requests cryptography pytest ruff
ruff check actions/oci-token-exchange/
pytest actions/oci-token-exchange/tests/ -v
actionlint .github/workflows/test-actions.yml
cd actions/run-oci-cli-command
npm ci
npm test -- --run
npm run typecheck
npm audit --audit-level=high
npm run licenses
npm run build
git diff --exit-code -- dist/index.js
```

If a command fails because an external service is temporarily unavailable, record the exact failure and retry once. Do not mark the plan complete with failing local gates.
  </action>
  <verify>
    <automated>python -m pip install 'oci>=2.173.1,<3' requests cryptography pytest ruff && ruff check actions/oci-token-exchange/ && pytest actions/oci-token-exchange/tests/ -v && actionlint .github/workflows/test-actions.yml && cd actions/run-oci-cli-command && npm ci && npm test -- --run && npm run typecheck && npm audit --audit-level=high && npm run licenses && npm run build && git diff --exit-code -- dist/index.js</automated>
  </verify>
  <acceptance_criteria>
    - `ruff check actions/oci-token-exchange/` exits 0
    - `pytest actions/oci-token-exchange/tests/ -v` exits 0
    - `actionlint .github/workflows/test-actions.yml` exits 0
    - `cd actions/run-oci-cli-command && npm test -- --run` exits 0
    - `cd actions/run-oci-cli-command && npm run typecheck` exits 0
    - `cd actions/run-oci-cli-command && npm audit --audit-level=high` exits 0
    - `cd actions/run-oci-cli-command && npm run licenses` exits 0
    - `git diff --exit-code -- actions/run-oci-cli-command/dist/index.js` exits 0 after build
  </acceptance_criteria>
</task>

</tasks>

<verification>
Plan passes when `.planning/REQUIREMENTS.md` and `.planning/ROADMAP.md` describe external smoke truthfully, `.github/workflows/test-actions.yml` has SHA pins/cache/checksum/local-readiness checks, and the local Python/npm/workflow verification suite exits 0.
</verification>

<success_criteria>
- SMOKE-01, SMOKE-06, SMOKE-07, and REL-03 are covered by local release-readiness work.
- No local `.github` workflow pretends to run real OCI token exchange from this repo.
- All third-party workflow action references are full SHA-pinned.
- Actionlint tarball installation verifies SHA256 before extraction.
- Existing Phase 1 and Phase 2 gates remain present and passing.
</success_criteria>
