---
phase: 03-smoke-test-branch-protection-and-v1-0-0-tag
plan: 04
type: execute
wave: 3
depends_on:
  - "03-03"
files_modified:
  - .planning/phases/03-smoke-test-branch-protection-and-v1-0-0-tag/03-EXTERNAL-SMOKE-EVIDENCE.md
autonomous: false
requirements_addressed: [REL-01, SMOKE-07]
requirements:
  - REL-01
  - SMOKE-07

must_haves:
  truths:
    - "Addresses review concern: final tag execution is isolated in this manual plan after the release branch has landed on main and colour-within-ops has smoke-tested the exact origin/main SHA."
    - "D-25 D-26 D-27: v1.0.0 is manual, requires external smoke evidence, and must not be created automatically."
    - "Candidate SHA in 03-EXTERNAL-SMOKE-EVIDENCE.md must equal git rev-parse origin/main before tagging."
    - "After tag creation, git rev-list -n 1 v1.0.0 must equal the candidate SHA smoke-tested by colour-within-ops."
    - "Unsigned v1.0.0 tags are forbidden; if signed tag creation fails, stop and surface GPG/signing misconfiguration."
  artifacts:
    - path: ".planning/phases/03-smoke-test-branch-protection-and-v1-0-0-tag/03-EXTERNAL-SMOKE-EVIDENCE.md"
      provides: "Passed external smoke evidence for exact origin/main candidate SHA"
      contains: "Status: passed"
---

<objective>
Create and push the signed `v1.0.0` tag only after the release branch has landed on `main`, the exact `origin/main` SHA has been smoke-tested by `colour-within-ops`, local branch protection has been verified, and the user gives final approval.
</objective>

<execution_context>
@/Volumes/External/ColourWithin/dot-github/.codex/get-shit-done/workflows/execute-plan.md
@/Volumes/External/ColourWithin/dot-github/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-smoke-test-branch-protection-and-v1-0-0-tag/03-RELEASE-DANCE.md
@.planning/phases/03-smoke-test-branch-protection-and-v1-0-0-tag/03-EXTERNAL-SMOKE-EVIDENCE.md
@README.md
@docs/actions/oci-ipt-smoke.md
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-3-12 tag targets a different commit than the one smoked by ops | high | Compare evidence candidate SHA, `git rev-parse origin/main`, and `git rev-list -n 1 v1.0.0` after tag creation |
| T-3-13 tag already exists locally or remotely | high | Run `git fetch --tags origin`, `git rev-parse v1.0.0`, and `git ls-remote --tags origin v1.0.0` before approval |
| T-3-14 unsigned release tag weakens release provenance | high | Use `git tag -s -a`; if signing fails, stop instead of falling back to unsigned tag |
| T-3-15 user approval skipped | high | Plan is `autonomous: false`; executor asks for explicit approval after all evidence checks pass and before tag command |
</threat_model>

<decision_coverage>
- D-25, D-26, and D-27 are covered by the manual final tag checkpoint.
- D-15 is covered by requiring passed external `colour-within-ops` smoke evidence before tag.
</decision_coverage>

<tasks>

<task type="manual">
  <name>Task 1: Verify post-merge candidate SHA and external smoke evidence</name>
  <files>.planning/phases/03-smoke-test-branch-protection-and-v1-0-0-tag/03-EXTERNAL-SMOKE-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/03-smoke-test-branch-protection-and-v1-0-0-tag/03-RELEASE-DANCE.md
    - .planning/phases/03-smoke-test-branch-protection-and-v1-0-0-tag/03-EXTERNAL-SMOKE-EVIDENCE.md
  </read_first>
  <action>
Fetch main and tags, then verify the release candidate SHA:

```bash
git fetch origin main --tags
CANDIDATE_SHA=$(git rev-parse origin/main)
```

Check `.planning/phases/03-smoke-test-branch-protection-and-v1-0-0-tag/03-EXTERNAL-SMOKE-EVIDENCE.md`:
- `Status:` is `passed`
- `Candidate .github SHA:` equals `$CANDIDATE_SHA`
- workflow run URL is filled in
- command is `oci os ns get` unless a documented fallback is used
- `exit-code` is `0`
- `oci-cli-version` is `>=3.81.1`
- notes say no deploy or mutation step ran

If any field is missing or mismatched, stop and ask the user to run or correct the external smoke evidence before continuing.
  </action>
  <verify>
    <manual>`03-EXTERNAL-SMOKE-EVIDENCE.md` status is `passed` and candidate SHA equals `git rev-parse origin/main`.</manual>
  </verify>
  <acceptance_criteria>
    - `03-EXTERNAL-SMOKE-EVIDENCE.md` contains `Status: passed`
    - Evidence candidate SHA equals `git rev-parse origin/main`
    - Evidence includes a workflow run URL
    - Evidence records `exit-code: 0`
    - Evidence records `oci-cli-version`
  </acceptance_criteria>
</task>

<task type="manual">
  <name>Task 2: Verify v1.0.0 tag does not already exist</name>
  <files>.planning/phases/03-smoke-test-branch-protection-and-v1-0-0-tag/03-EXTERNAL-SMOKE-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/03-smoke-test-branch-protection-and-v1-0-0-tag/03-RELEASE-DANCE.md
  </read_first>
  <action>
Run:

```bash
git fetch --tags origin
git rev-parse v1.0.0
git ls-remote --tags origin v1.0.0
```

Both tag lookup commands should fail or return empty before creating the release tag. If either command finds an existing `v1.0.0`, stop and ask the user whether the release already happened or whether a recovery plan is needed.
  </action>
  <verify>
    <manual>No local or remote `v1.0.0` tag exists before final approval.</manual>
  </verify>
  <acceptance_criteria>
    - `git rev-parse v1.0.0` fails before tag creation
    - `git ls-remote --tags origin v1.0.0` returns no `refs/tags/v1.0.0` before tag creation
  </acceptance_criteria>
</task>

<task type="manual">
  <name>Task 3: Stop for explicit approval, then create signed v1.0.0 tag</name>
  <files>.planning/phases/03-smoke-test-branch-protection-and-v1-0-0-tag/03-EXTERNAL-SMOKE-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/03-smoke-test-branch-protection-and-v1-0-0-tag/03-EXTERNAL-SMOKE-EVIDENCE.md
  </read_first>
  <action>
Stop and ask the user for explicit approval to create and push `v1.0.0` at the verified candidate SHA. After approval, run:

```bash
git tag -s -a v1.0.0 "$CANDIDATE_SHA" -m "v1.0.0"
git push origin v1.0.0
git rev-list -n 1 v1.0.0
git ls-remote --tags origin v1.0.0
```

If `git tag -s` fails because signing is unavailable, stop. Do not create an unsigned `v1.0.0` tag. Surface the signing/GPG failure to the user.
  </action>
  <verify>
    <manual>`git rev-list -n 1 v1.0.0` equals `$CANDIDATE_SHA`, and `git ls-remote --tags origin v1.0.0` shows the pushed tag.</manual>
  </verify>
  <acceptance_criteria>
    - User explicitly approves creating `v1.0.0`
    - `git tag -s -a v1.0.0 "$CANDIDATE_SHA" -m "v1.0.0"` exits 0
    - `git push origin v1.0.0` exits 0
    - `git rev-list -n 1 v1.0.0` equals the candidate SHA smoke-tested by `colour-within-ops`
    - `git ls-remote --tags origin v1.0.0` shows `refs/tags/v1.0.0`
  </acceptance_criteria>
</task>

</tasks>

<verification>
Plan passes only when `v1.0.0` is signed, pushed, and points to the exact candidate SHA that `colour-within-ops` smoke-tested.
</verification>

<success_criteria>
- REL-01 is satisfied by a signed `v1.0.0` tag pushed to origin.
- The tag target equals the external-smoke candidate SHA.
- No unsigned fallback tag is created.
- User approval happens immediately before tag creation.
</success_criteria>
